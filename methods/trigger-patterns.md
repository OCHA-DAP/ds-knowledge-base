# Trigger patterns — typology

> **Status: stub — emerges bottom-up.** This is the highest-value methods page: the living typology of *how we actually build triggers*, written once here so framework pages can link to a pattern instead of re-explaining it. It gets populated during the diverse-sample ingestion (Phase 1), not pre-filled now.

Each pattern, once observed in ≥2 frameworks, gets a short write-up:

- **Name** (the `trigger_facets.structure` value or a coined term)
- **Shape** — the decision logic in the abstract
- **When it's used** — hazard / data / context it fits
- **Calibration approach** — how thresholds get set for it
- **Frameworks using it** — links to the framework pages

## Patterns observed so far

_(none yet — populate during Phase 1)_

<!-- candidate buckets to confirm against real frameworks:
  - single forecast-probability threshold (e.g. tercile below-normal > X)
  - multi-window seasonal monitoring
  - readiness → activation (two-stage)
  - return-period discharge / observational exceedance
  - wind-buffer exposure (cyclones)
  - bespoke / composite
-->
