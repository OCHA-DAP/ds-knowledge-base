# Catalog — all framework-versions

> **Generated file — do not hand-edit.** Built from the frontmatter of every `frameworks/**/*.md` page. Regenerated after ingestion (Phase 3+).

Once populated, this is the filterable cross-framework view: every version with its hazard, country, data sources, trigger facets, status, and links — so you can answer "show me every drought framework on SEAS5" or "every readiness-activation trigger" by reading one table.

_(empty until the first ingestion runs)_
