---
content_type: framework
framework: nga-flooding
version: "2026-06-18"
status: endorsed
valid_until: null   # no published 2026 doc states a validity period for THIS version. The predecessor (2025-08-11) states "valid for a period of two years (2025 and 2026)"; not carried over here without a doc.
country_iso3: NGA
hazard: flood
admin_level: 2
geographic_scope:
  - NG002001
  - NG002002
  - NG002005
  - NG002009
  - NG002016
  - NG002020
  - NG002021
data_sources:
  - GloFAS
  - Google-GRRR
  - FloodScan-SFED
  - NiHSA
  - WorldPop
trigger_facets:
  basis: mixed
  calibration: return-period
  indicators:
    - Google-GRRR-multi-gauge-discharge
    - GloFAS-discharge
    - FloodScan-SFED
  n_windows: 3
  window_axes:
    - time
    - space
monitoring_period:
  months: [8, 9, 10, 11]
  source: inferred
  note: "Wet season Aug–Nov inferred from the wet-season annual-maxima calibration underlying ACTION_GAUGE_THRESHOLDS/READINESS_GLOFAS_THRESH in src/constants.py (comment cites exploration/2026/cerf/workflow/06_trigger_definition.ipynb, not present in this checkout — see discrepancies). Action (GRRR) and readiness (GloFAS, forecast OR reanalysis) riverine triggers both monitor any wet-season day; the NHF flash-flood window is observational over the same season."
supersedes: "2025-08-11"
# --- funding & scope ---
all_in: false   # split: CERF riverine windows vs a separate NHF flash-flood stream
prearranged_funding_usd: null
funding_by_source: {}
funding_by_sector: {}
funding_by_agency: {}
funding_rows: []
cofinancing_usd: null
cofinancing_sources: []
implementing_agencies: []
target_people: null
# --- documents, authority-ranked ---
framework_doc: null
framework_doc_date: null
framework_doc_annexes: []
languages:
  - en
model_report: null
raw_extract: []
# --- live system ---
operated_by: null
apps:
  - "https://nga-floodexposure-marim-development-guc4dqhrguaxf3d9.eastus2-01.azurewebsites.net/"
  - "https://nga-monitoring-marimo-development-d6fhc2fud0hqa3e6.eastus2-01.azurewebsites.net/"
depends_on:
  - listmonk
# --- source repo & reconciliation ---
source_repo: ocha-dap/ds-aa-nga-flooding
source_branch: feat/2026-trigger-monitoring
source_sha: 0b763c3
code_ref:
  - src/constants.py
  - src/datasources/glofas.py
  - src/datasources/grrr.py
  - src/monitoring/etl.py
  - pipelines/check_forecasts.py
  - pipelines/save_plots.py
  - pipelines/send_emails.py
  - pipelines/setup_nga_listmonk_lists.py
  - .github/workflows/monitoring.yml
trigger_source: repo
repo_completeness:
  analysis: partial
  deployed_code: partial
discrepancies:
  - "[gap] No PDF extract was available for this re-sync (framework_doc stays null — pending publication per the prior sync's provenance). This page is drafted entirely from the repo; every design claim below is `trigger_source: repo`, not doc-verified."
  - "[gap] The endorsed 2026 trigger is implemented ONLY on the unmerged branch `feat/2026-trigger-monitoring` (PR #37, still OPEN against `main` as of 2026-08-08): commit 79fd5d7 switched monitoring to the multi-gauge action + GloFAS readiness design; 5d44ff8 added the GloFAS-reanalysis OR-branch to readiness 'per the framework doc' (resolving IN CODE the prior sync's [conflict] between repo and doc on this point); 2e5818f switched email dispatch to Listmonk/ocha-relay; ff36c17 + 0b763c3 adapted to a restructured `cems-glofas-historical` schema (GloFAS v5 rollout, Aug 2026) and bumped eccodes. `main` is still at f4e058c, i.e. the 2025 single-station code — nothing of the 2026 design is on the default branch."
  - "[gap] The design notebooks the code comments cite as their source (`exploration/2026/cerf/workflow/06_trigger_definition.ipynb`, `07_readiness_trigger.ipynb`) — and the session notes the prior sync used as its narrative source (`notes/2026-06-18.md`) — do NOT exist anywhere in this checkout; `exploration/` here only has `2024/` and `2025/` subdirectories. This is worse than the prior sync's finding (which had the notebooks, just not the consolidated write-ups): the `exploration/2026/` tree lives on a different, still-unmerged branch (`feat/niger-benue-multistate-monitoring`, referenced by `deploy-app-cron.yml`'s checkout `ref:`), not on `feat/2026-trigger-monitoring`. QA note (2026-08-08): the figures below WERE cross-checked against `exploration/2026/cerf/notes/2026-06-18.md` on that sibling branch — the 10-gauge table, per-gauge 4-yr thresholds, action RP 4.5 yr, readiness option-A RP 3.0 yr / 3,132 m³/s / LT≤13d and the ≤7-day GRRR reforecast horizon all match `src/constants.py`. The gap is that they are unreachable from THIS branch, not that they are unverified."
  - "[resolved 2026-08-10] The 2026 monitoring is LIVE from main: PR #38 (cron shim) merged and ran in soak mode, then PR #37 merged to main on 2026-08-10, monitoring.yml re-enabled (was disabled_inactivity) and the shim deleted — monitoring.yml's own 20:00 UTC daily schedule is now the single cron. STAGE repo var = prod: real (non-test) Listmonk emails to the prod lists, which deliberately hold only the two team members (soak-test audience) pending full distribution-list migration. DB writes remain dev-stage."
  - "[gap] The whole data plane is hardcoded to dev, independent of the `STAGE` env var that gates email routing: DB writes (`stratus.get_engine(stage=\"dev\", write=True)` in `check_forecasts.py`), DB reads (`stratus.get_engine(stage=\"dev\")` in `etl.get_database_forecast`) and the GloFAS blob container (`stratus.get_container_client(\"projects\", \"dev\")` in `etl.get_glofas_forecast`/`get_glofas_reanalysis`). Flipping `STAGE=prod` for real sends will NOT move monitoring data off the dev database/container unless these are separately changed."
  - "[resolved 2026-08-11] The 2026 NHF flash-flood config is now fixed and LIVE (repo PR #39): 4 LGAs from the NiHSA composite retargeting (Mobbar NG008023, Maiduguri NG008021, Jere NG008013, Geidam NG036006 — selection rationale in notes/trigger_development.md on govt-2026-prep) with per-LGA exposure thresholds from the framework document (FLASH_LGAS in src/constants.py), monitored daily. The legacy NHF_FLASH_LGAS (2025 six-LGA set) remains in constants for historical analysis only."
  - "[gap] The whole-Nigeria GloFAS reforecast bulk-download pipeline (`download_glofas_reforecast_country.py`) recorded by the prior sync is NOT on this branch — that work (branch `feat/glofas-country-reforecast-download`) was merged into a third branch, `feat/niger-benue-multistate-monitoring` (commit 071ab76), not into `feat/2026-trigger-monitoring`. Dropped from `code_ref` for this version; see that other branch if it's re-synced separately."
  - "[stale] `GLOFAS_THRESH=3132`, `GOOGLE_THRESH=1195`, `GLOFAS_WARNING_THRESH=2800`, `GOOGLE_WARNING_THRESH=1000` in `src/constants.py` are the 2025 single-station thresholds, retained for historical notebooks — not the live 2026 design (superseded in the same file by `ACTION_GAUGE_THRESHOLDS`/`READINESS_GLOFAS_THRESH`)."
  - "[stale] Benue — `BENUE_ADM2_PCODES` in `src/constants.py` is a legacy variable name for the 7 Benue-*river*/Adamawa-*state* LGAs (the same 7 pcodes as `geographic_scope`), not Benue *state*; Benue state itself was dropped from the 2026 framework (per the prior sync) and has no separate config on this branch."
  - "[stale] Listmonk migration is incomplete on the template side: `send_emails.py`'s docstring says the Jinja templates provide 'only the content fragment' with branding/footer/unsubscribe from the Listmonk `base_campaign` template, but `action.html`/`readiness.html`/`informational.html` still `{% extends \"base.html\" %}`, and that local `base.html` still renders its own H1, status header and a hand-maintained contact/add-remove footer — duplicating what the instance template supplies."
  - "[stale] The deployed marimo apps (`nga-monitoring-marimo`, `nga-floodexposure-marimo`) still deploy to the Azure 'development' slot only (`slot-name: 'development'` in both `main_nga-*-marimo(development).yml` workflows on this branch) — unchanged from the prior sync."
activations: []
# --- escape hatch ---
extra:
  dev_status: "ENDORSED (per the prior sync's 2026-07-29 relay from the framework team); the framework document is finalised but framework_doc stays null pending publication (no PDF extract available for this re-sync either — this page is drafted entirely from the repo). The 2026 trigger design (multi-gauge action + GloFAS readiness with the reanalysis OR-branch + Listmonk dispatch) is fully implemented and, as of 2026-08-10, MERGED to main (PR #37) and running on monitoring.yml's own daily 20:00 UTC schedule with STAGE=prod (prod audience deliberately limited to two team members pending distribution-list migration). Charts restyled with HDX design tokens."
  action_trigger_design: "Multi-gauge GRRR trigger: fires if ≥6 of 10 selected GRRR gauges simultaneously exceed their individual 4-yr empirical Weibull RP threshold on at least one wet-season day (Aug–Nov). Implemented in `src/constants.py` (`ACTION_GAUGE_THRESHOLDS`, `ACTION_MIN_GAUGES=6`) and evaluated by `src/monitoring/etl.evaluate_trigger` (max simultaneous per-day exceedance count across all 10 gauges, fetched in one Google API call per `check_forecasts.py`). Per-gauge thresholds ~1,101–1,117 m³/s for 9 main-channel gauges; one outlier (`hybas_1120840690`) at 142.7 m³/s. Design provenance (10 gauges selected by Spearman ρ against FloodScan SFED Aug–Nov with lags −7..+14 d; reanalysis fire years 1998, 1999, 2012, 2018, 2019, 2022; GRRR reforecast horizon ≤7 d) is not in this checkout but was confirmed on the sibling branch's `exploration/2026/cerf/notes/2026-06-18.md`, whose per-gauge threshold table matches `ACTION_GAUGE_THRESHOLDS` exactly (see discrepancies)."
  readiness_trigger_design: "GloFAS ensemble reforecast mean > 3,132 m³/s at Wuroboki (G5004), lead time ≤13 days (`READINESS_GLOFAS_THRESH=3132.0`, `READINESS_MAX_LEADTIME=13` in `src/constants.py`), OR the latest GloFAS reanalysis exceeding the same threshold. This is 'option A' from the design note (RP 3.0 yr, POD 0.75 vs the four action years with reforecast coverage, FPR 25%); option B (3,250 m³/s, LT≤12 d, RP 3.5 yr) was the rejected alternative. The reanalysis OR-branch is now IN CODE: commit 5d44ff8 ('include GloFAS reanalysis in the readiness trigger (per the framework doc)') added it to `etl.evaluate_trigger` (`readiness = readiness_forecast | readiness_reanalysis`) and `check_forecasts.py` fetches both the forecast and a reanalysis (walking back from −2 to −7 days to find an available reanalysis date; a missing reanalysis logs a warning but never breaks the run). NOTE: the prior sync recorded the repo analysis as having *recommended against* this branch — that claim could NOT be substantiated in the (public) sibling-branch notebooks/notes, which evaluate readiness purely as a reforecast trigger and elsewhere prepend reanalysis as leadtime=0; treat the 'evaluated and rejected' framing as unsourced. Commit ff36c17 subsequently adapted the reanalysis fetch to a restructured `cems-glofas-historical` CDS schema (GloFAS v5 rollout, Aug 2026: year/month/day replacing hyear/hmonth/hday, a renamed discharge variable, a required `timespan`) while explicitly pinning `system_version: version_4_0` — the 3,132 m³/s threshold is calibrated on the v4 reanalysis climatology, so the request keeps requesting v4 data from the restructured endpoint."
  nhf_flash_flood: "Observational flash-flood window, NHF-funded, distinct hazard type and funding mechanism from the CERF riverine triggers. 2026 config (resolved 2026-08-11, live via repo PR #39): 4 LGAs selected by NiHSA 2026 composite at-risk-community score — Mobbar, Maiduguri, Jere (Borno), Geidam (Yobe). Indicator: FloodScan SFED x WorldPop population exposure per LGA (from the floodexposure-monitoring pipeline, DB app.floodscan_exposure prod), STRICT 3-day rolling mean. Per-LGA thresholds from the framework document — Mobbar 14,720; Maiduguri 23,740; Jere 103,612; Geidam 5,501 people exposed — independently validated: each reproduces as the empirical Weibull ~7.75-yr RP of calendar-year annual maxima (1998–2025) on exactly the operational data/aggregation, i.e. exactly 3 exceedance years per LGA (equal per-LGA probability by design); combined any-LGA trigger fires 8/28 years (~3.5-yr RP: 1999, 2000, 2006, 2011, 2020, 2021, 2022, 2024). An advisory tier emails at 80% of any threshold (FLASH_WARNING_FRACTION)."
  listmonk_dispatch: "Email dispatch (`pipelines/send_emails.py`) now sends via Listmonk (`ocha-relay`'s `ListmonkClient`) instead of raw SMTP, per commit 2e5818f — three lists resolved by tag at runtime (never hardcoded id): `nga:trigger` — ACTION alerts only; `nga:info` — both the readiness alert and the routine informational email; `nga:test`. Lists are created/populated by `pipelines/setup_nga_listmonk_lists.py` (idempotent, matches by tag/email; can migrate the legacy blob distribution-list CSV). When `STAGE != prod` (the default), ALL sends route to the test list regardless of trigger status, with a `[TEST]` subject prefix and a `[test]`-flagged campaign name (switches on the Listmonk `base_campaign` template's test banner). The module docstring describes the templates (`action.html`/`readiness.html`/`informational.html`) as content fragments with branding/footer/unsubscribe from the Listmonk instance template, but in practice all three still extend a local `base.html` carrying its own header and contact footer (see discrepancies)."
  glofas_v5_schema_change: "GloFAS rolled out a v5 reanalysis product in Aug 2026 that restructured the `cems-glofas-historical` CDS dataset (commit ff36c17): request fields changed from `hyear/hmonth/hday` to `year/month/day`, the discharge variable was renamed, and a `timespan` field became required. `src/monitoring/etl.get_glofas_reanalysis` was updated to the new schema (`product_type: intermediate`, `timespan: time_mean`, `variable: average_river_discharge_in_the_last_24_hours`; `process_glofas` now picks the single data variable generically rather than by name) while pinning `system_version: version_4_0` so the readiness threshold's v4 calibration stays valid. Commit 0b763c3 additionally bumped the `eccodes` GRIB library (2.47.0, bundled lib 2.48) to decode a new CEMS GRIB local-section introduced alongside the schema change."
  branch_topology_note: "This repo currently has at least three parallel, still-unmerged feature lines relevant to this framework, which this sync did NOT combine: (1) `feat/2026-trigger-monitoring` (this page's source — trigger design + monitoring pipeline; PR #37 MERGED to main 2026-08-10, branch deleted), (2) `feat/niger-benue-multistate-monitoring` (holds the `exploration/2026/` + `exploration/2026/cerf/` design notebooks, notes incl. `notes/2026-06-18.md`, a multi-state trigger config, and the GH Pages monitoring app at https://ocha-dap.github.io/ds-aa-nga-flooding/ — commit 80ccc0b; it is the branch `deploy-app-cron.yml` on `main` checks out every 6 h, so it IS the one thing currently running), and (3) `feat/glofas-country-reforecast-download` (whole-Nigeria GloFAS bulk-download pipeline, PR #35 open against `govt-2026-prep`, and separately merged into branch (2) at commit 071ab76 — verified as an ancestor of that branch — not into this one). Plus (4) `chore/monitoring-2026-cron-shim` (PR #38 merged 2026-08-10; shim since deleted from main after PR #37 landed). A future re-sync against (2) would recover the design notebooks and the NHF retargeting work."
  schema_strain: "n_windows=3 = action (GRRR multi-gauge riverine) + readiness (GloFAS riverine, forecast OR reanalysis) + NHF flash flood (observational, BAY states, separate NHF funding). window_axes=[time, space]: action vs readiness differ by forecast role/timing; NHF differs by geography and hazard sub-type. basis=mixed because the riverine windows are forecast-based while the NHF window is observational."
visibility: internal
last_synced: "2026-08-10"
---

# Nigeria Flood — 2026-06-18 (endorsed)

> **Endorsed** successor to the 2025-08-11 version. The framework document is finalised but **not yet published** (`framework_doc` stays null; no PDF extract was available for this re-sync either — this page is drafted entirely from the repo at `source_branch: feat/2026-trigger-monitoring`, `source_sha: 0b763c3`). Since the prior sync (SHA `fcbe009`, branch `feat/glofas-country-reforecast-download`), the design has moved from notebooks into working code on a different branch — but the design notebooks themselves are no longer reachable from this checkout, and none of the 2026 code is on a schedule yet (see discrepancies). The canonical trigger is the code at `code_ref`; this page explains it, it does not redefine it.

## Summary

This is the endorsed 2026 revision of Nigeria's flood anticipatory action framework, focused on **Adamawa state** along the Benue river. The action trigger is a **multi-gauge consensus design**: fires when ≥6 of 10 selected Google GRRR gauges simultaneously exceed their individual 4-year return-period threshold, replacing the 2025 single-station OR logic (GloFAS OR GRRR at one Wuroboki gauge). A **GloFAS readiness trigger** (ensemble reforecast mean > 3,132 m³/s at Wuroboki, lead time ≤13 days) gives partners advance notice ahead of the action trigger, **OR**-ed with a GloFAS reanalysis safety-net branch — this OR-branch, previously only in the (unpublished) framework document while the repo analysis recommended against it, is now implemented in code. The **NHF flash-flood** component (observational, BAY states) is retargeted for 2026 to 4 LGAs (Mobbar, Maiduguri, Jere, Geidam — NiHSA composite score) with new per-LGA FloodScan-exposure thresholds from the framework document, validated against the operational data (equal per-LGA probability, combined ~3.5-yr RP). Email dispatch now runs through **Listmonk/ocha-relay** rather than raw SMTP. No funding envelope, implementing agencies, or target population are recorded (the framework document is pending publication). As of **2026-08-10/11 the full 2026 design is merged to `main` and live**: `monitoring.yml` (riverine) runs daily at 20:00 UTC and `flash-monitoring.yml` runs daily at 01:30 UTC (timed to the floodexposure chain), both with `STAGE=prod`; each stream's production audience is deliberately limited to two team members (soak test) pending migration of the full distribution list.

## Method

The repo implements the trigger directly in `src/constants.py` and evaluates it via `pipelines/check_forecasts.py` → `src/monitoring/etl.evaluate_trigger` → `pipelines/send_emails.py` (intended daily; see Monitoring for why it isn't). The structured workflow notebooks that derived the design (gauge selection, RP calibration, readiness lead-time evaluation — `exploration/2026/cerf/workflow/01`–`07`) are **not present on this branch**; they sit on the separate, also-unmerged `feat/niger-benue-multistate-monitoring` (confirmed present there — it is the branch `deploy-app-cron.yml` checks out). This page therefore describes the trigger as implemented in `src/constants.py`/`etl.py`, with the design rationale cross-checked against that branch's session note.

The selected Adamawa action trigger: **≥6 of 10 GRRR gauges simultaneously exceed their individual 4-yr empirical Weibull RP threshold on at least one wet-season day** (per-gauge thresholds ~1,101–1,117 m³/s for 9 main-channel gauges, 142.7 m³/s for the outlier). The readiness trigger combines a GloFAS reforecast branch (Option A) with a reanalysis OR-branch, both evaluated against 3,132 m³/s. The observational NHF flash-flood window is retargeted to 4 BAY-state LGAs with framework-document exposure thresholds (see `extra.nhf_flash_flood`).

## Trigger logic

- **Keys off:** Google GRRR reforecast streamflow at 10 gauges in Adamawa (action); GloFAS ensemble reforecast mean AND latest GloFAS reanalysis at Wuroboki (readiness); FloodScan SFED x WorldPop per-LGA population exposure (NHF flash flood).
- **Decision rule (plain language):** The **action** trigger fires if at least 6 of the 10 selected GRRR gauges simultaneously report streamflow above their individual 4-year return-period threshold on the same forecast valid day. The **readiness** trigger fires when either the GloFAS ensemble-mean forecast at Wuroboki exceeds 3,132 m³/s at a lead time of ≤13 days, **or** the latest available GloFAS reanalysis (fetched by walking back from −2 to −7 days to find an available date) exceeds the same threshold — an earlier, lower-bar pre-positioning signal that does not guarantee an action activation will follow. The reanalysis branch is implemented in code (commit 5d44ff8) as a near-real-time safety net for reforecast misses, "per the framework doc" in the commit's own words. The **NHF flash-flood** window fires when the 3-day rolling mean of FloodScan-derived population exposure in any of the 4 retargeted LGAs (Mobbar, Maiduguri, Jere, Geidam) exceeds its per-LGA threshold; an advisory email goes out at 80% of any threshold.
- **Activation structure:** Two riverine windows for Adamawa — readiness (GloFAS, intended to fire first) and action (GRRR multi-gauge) — plus the parallel NHF flash-flood window. Emails distinguish three states (`action` > `readiness` > "not activated"), sent via Listmonk whenever a trigger fires or it's a Monday. Only the action state routes to the `nga:trigger` list; readiness goes to `nga:info` alongside the routine update.
- **Calibration:** Empirical Weibull RP from wet-season (Aug–Nov) annual maxima, 1998–2023 (GRRR) / 2003–2022 (GloFAS reforecast). Action trigger **4.5-yr** RP (k=6 fire years in n=26); readiness option A **3.0-yr** RP (k=7 in n=20). Verified against the design note on the sibling branch (`exploration/2026/cerf/notes/2026-06-18.md`), which is not reachable from this checkout — see discrepancies. Readiness POD vs the four action years with reforecast coverage is 0.75 (misses 2018); lead time over the action trigger is highly uneven (+46 d in 2019, +2 to +13 d in 2022/2012).
- **Authoritative source:** `trigger_source: repo` — no published framework document exists yet (`framework_doc: null`); no PDF extract was available for this re-sync either, so this page is drafted entirely from `src/constants.py`, `src/monitoring/etl.py`, and the pipeline/workflow code.
- **Operated by:** null — analysis and monitoring code by OCHA CHD, intended to run via GitHub Actions in this repo, not a third-party system. As of `source_sha` neither the branch workflow nor the pre-merge shim is actually scheduled (see Monitoring).

## Trigger windows

| window | basis | indicator | threshold | lead time | return period | releases |
|---|---|---|---|---|---|---|
| Adamawa action — riverine (Benue R.) | forecast (GRRR reforecast) | Google-GRRR-multi-gauge-discharge (≥6 of 10 gauges simultaneously exceed 4-yr RP) | Per-gauge 4-yr empirical Weibull RP (~1,101–1,117 m³/s for 9 gauges; 142.7 m³/s for outlier `hybas_1120840690`) — `ACTION_GAUGE_THRESHOLDS`/`ACTION_MIN_GAUGES=6` in `src/constants.py` | GRRR reforecast horizon ≤7 days | 4.5 yr (Weibull, 6 fire years in 1998–2023) | Not yet defined (2025 equivalent released CERF $5M) |
| Adamawa readiness — GloFAS (forecast OR reanalysis) | forecast (GloFAS reforecast ensemble mean) OR near-real-time reanalysis | GloFAS-discharge at Wuroboki (G5004) | ≥3,132 m³/s (`READINESS_GLOFAS_THRESH`) for both branches | ≤13 days (`READINESS_MAX_LEADTIME`, forecast branch); reanalysis fetched by walking back −2 to −7 days | 3.0 yr (Weibull, 7 fire years in 2003–2022) | Not yet defined; low-cost early pre-positioning before action |
| NHF flash flood — BAY states (4 retargeted LGAs) | observational | FloodScan×WorldPop population exposure per LGA, strict 3-day rolling mean (`app.floodscan_exposure`) | Mobbar 14,720; Maiduguri 23,740; Jere 103,612; Geidam 5,501 people exposed (framework doc; validated ≈7.75-yr RP each, 3/28 exceedance years) | n/a (observational; advisory at 80%) | per-LGA ~9.3 yr; combined any-LGA ~3.5 yr | Separate NHF stream (2025 equivalent ~$2M) |

## Per-country variants

*(Single-country framework — section not applicable.)*

## Sources & repo completeness

- **Trigger taken from:** `trigger_source: repo` — no published framework document exists yet for this version. No PDF extract was available for this re-sync; all content here comes from `src/constants.py`, `src/monitoring/etl.py`, and the monitoring pipelines/workflows on `feat/2026-trigger-monitoring` (sha `0b763c3`).
- **Repo completeness:** `{analysis: partial, deployed_code: partial}`. The trigger logic, thresholds, readiness reanalysis OR-branch, and Listmonk email dispatch are all fully implemented and, since 2026-08-10, deployed: PR #37 is merged and `monitoring.yml` runs daily from `main` with `STAGE=prod` (two-person soak audience). The data plane stays hardcoded to dev, and the design-derivation notebooks that justified the thresholds still live on a sibling unmerged branch. The NHF flash-flood 2026 config is implemented and live (`FLASH_LGAS`, `pipelines/monitor_flash_flood.py`); its threshold-derivation notebook is not in the repo (values from the framework document, validated in the PR #39 commit trail).
- **Discrepancies:** See frontmatter `discrepancies`. Headline: the 2026 design is further implemented in code than the prior sync recorded (reanalysis OR-branch, Listmonk dispatch, GloFAS v5 schema adaptation) and is **live since 2026-08-10** — though the design assets remain spread across sibling branches (`feat/2026-trigger-monitoring`, `feat/niger-benue-multistate-monitoring`, `feat/glofas-country-reforecast-download`, `chore/monitoring-2026-cron-shim`), with this sync having direct visibility into only the first.

## Monitoring

The daily pipeline (`pipelines/check_forecasts.py` → `save_plots.py` → `send_emails.py`, GHA `monitoring.yml`, cron `0 20 * * *`) implements the endorsed 2026 design — live from `main` since 2026-08-10. **Action check:** all 10 endorsed GRRR gauges fetched in one Google API call, per-gauge 4-yr RP thresholds hardcoded in `src/constants.py` (`ACTION_GAUGE_THRESHOLDS`), fires on ≥6 simultaneous same-valid-day exceedances (`etl.evaluate_trigger`). **Readiness check:** GloFAS forecast at Wuroboki to a 13-day lead (CDS `cems-glofas-forecast`) OR the latest available `cems-glofas-historical` reanalysis (walking back up to 7 days; a missing reanalysis logs a warning but never breaks the run), both vs 3,132 m³/s — the reanalysis fetch was recently adapted (commit ff36c17) to a restructured CDS schema from the Aug-2026 GloFAS v5 rollout, while staying pinned to `system_version: version_4_0` for calibration consistency. Emails go via **Listmonk/ocha-relay** (default `base_campaign` template; lists resolved by tag — `nga:info`, `nga:trigger`, `nga:test`; see `infrastructure/comms-listmonk.md`); `STAGE != prod` routes all sends to the test list with the template's test banner.

**Deployment status — LIVE (2026-08-10).** The path in: shim PR #38 merged 2026-08-10 morning and validated the full GHA chain in soak mode (one linux-only fix surfaced: eccodes/cfgrib segfault at interpreter teardown after successful completion — worked around with `os._exit(0)` in `check_forecasts.py`); PR #37 then merged to `main`, `monitoring.yml` was re-enabled (it had been `disabled_inactivity` since 2025-12-08), and the shim was deleted so exactly one cron runs: `monitoring.yml`, daily 20:00 UTC from `main`. `STAGE` repo var = `prod` → real Listmonk campaigns to the prod lists (`nga:info` 113 / `nga:trigger` 114), which deliberately contain only two team members as a soak-test audience; the test list (`nga:test` 115) remains for `STAGE=dev` runs. Charts are restyled with HDX design tokens. DB writes remain dev-stage.

**Flash-flood monitoring — LIVE (2026-08-11, repo PR #39).** A second daily pipeline (`pipelines/monitor_flash_flood.py`, GHA `flash-monitoring.yml`, cron `30 1 * * *` — timed to land right after the `floodexposure-monitoring` chain writes the day's exposure at ~23:40–23:52 UTC, worst observed slip ~01:10) reads per-LGA FloodScan×WorldPop exposure from `app.floodscan_exposure` (prod DB), applies the strict 3-day rolling mean matching the threshold derivation, and evaluates the 4 retargeted LGAs against their framework thresholds. A freshness guard fails the run loudly (no email) if the latest `valid_date` is older than run-date−2. Three email states: trigger reached (→ `nga-flash:trigger` list 119), approaching threshold at ≥80% (→ `nga-flash:info` 118, any day), weekly Monday informational; `STAGE != prod` routes to `nga-flash:test` (120). The flash stream's audience is separate from the riverine lists. Operational runbook for both pipelines: [pipelines/nga-flooding-monitoring.md](../../pipelines/nga-flooding-monitoring.md).

The data plane is hardcoded to dev regardless of `STAGE` — DB writes (`stratus.get_engine(stage="dev", write=True)` in `check_forecasts.py`), DB reads (`stratus.get_engine(stage="dev")` in `etl.get_database_forecast`) and the GloFAS blob container (`stratus.get_container_client("projects", "dev")`). This is independent of the email-routing flag and will not change when `STAGE` flips to `prod`.

This checkout does **not** contain the whole-Nigeria GloFAS reforecast bulk-download pipeline (`download_glofas_reforecast_country.py`) that the prior sync recorded — that work lives on `feat/glofas-country-reforecast-download`, later merged into a third branch (`feat/niger-benue-multistate-monitoring`), not into this one. The marimo apps (`nga-monitoring-marimo`, `nga-floodexposure-marimo`) still deploy to the Azure **`development`** slot only, per their workflow files on this branch. The one automation that *is* live is `deploy-app-cron.yml` on `main`, which every 6 h checks out `feat/niger-benue-multistate-monitoring` and republishes the GH Pages site (<https://ocha-dap.github.io/ds-aa-nga-flooding/>, HTTP 200) — that is the sibling branch's multi-state app, not this version's trigger monitoring. (Live health for deployed jobs is in the generated [pipeline-registry.md](../../infrastructure/pipeline-registry.md).)

## Historical activations

**Never activated under this version** (`activations: []`). The framework's only real activation to date is 8 September 2025, under the 2025-08-11 version and its single-station OR trigger — see [`2025-08-11.md`](2025-08-11.md). No 2026 activation is reported by OCHA, CERF or the Anticipation Hub as of 2026-08-08, and the 2026 trigger has in any case not been evaluated on a schedule (see Monitoring). Do not read the 2025 activation as evidence of how this version's multi-gauge trigger behaves; for that, the simulated record is the reanalysis fire years 1998, 1999, 2012, 2018, 2019, 2022 (6 in 26 years → 4.5-yr RP).

## Key decisions & rationale

**Multi-gauge consensus over single-gauge OR logic.** The 2025 trigger's single Wuroboki station was a single point of failure. Requiring 6 of 10 gauges to agree simultaneously is a spatial quality filter that eliminates local noise and ensures the signal represents a basin-wide flood, at the cost of losing GloFAS as an independent model cross-check inside the action trigger — which is part of why GloFAS was redeployed as a separate readiness trigger instead.

**GloFAS readiness at 3,132 m³/s / LT≤13 d (option A), plus a reanalysis safety net.** The design note weighs option A against option B (3,250 m³/s, LT≤12 d): A has the shorter RP (3.0 vs 3.5 yr) and higher POD (0.75 vs 0.50) at a higher false-alarm rate (25% vs 19%), and is the only one that catches 2012 — though only by +2 days, effectively a nowcast. Neither option detects 2018 at any practical threshold within LT≤13 d, a GloFAS forecasting limitation rather than a threshold choice. Commit 5d44ff8 then added a reanalysis OR-branch against the same threshold "per the framework doc", so `evaluate_trigger` ORs a forecast-branch check with a near-real-time reanalysis check. (The prior sync framed this branch as something the repo analysis had evaluated and rejected; that framing could not be substantiated in the public notebooks — see `extra.readiness_trigger_design`.)

**Listmonk over raw SMTP.** Commit 2e5818f moved email dispatch to the team's shared Listmonk/ocha-relay convention (`infrastructure/comms-listmonk.md`) — tag-resolved lists instead of hardcoded ids, a shared branding/footer template, and a built-in test-mode banner, replacing bespoke SMTP + blob-CSV distribution-list plumbing.

**Adapting to the GloFAS v5 schema change without breaking calibration.** When CEMS restructured the `cems-glofas-historical` dataset in Aug 2026, the fetch code was updated to the new request schema but deliberately kept requesting `system_version: version_4_0` data — the 3,132 m³/s threshold's calibration is anchored to v4 reanalysis climatology, so silently picking up v5 data would have invalidated the threshold without a recalibration.

**Trying to ship ahead of the merge — but the shim didn't land.** Rather than wait for PR #37 to reach `main` (where GitHub Actions schedules must live), the team wrote a disposable shim workflow that checks out the feature branch directly, mirroring the pattern that already works for `deploy-app-cron.yml`. The shim was opened as its own PR (#38) and is still unmerged, so it inherits exactly the problem it was meant to solve: a `schedule:` on a non-default branch never fires. The 2026 design is therefore still untested in operation.

## Changes from previous version

The 2025-08-11 endorsed version uses a **single-station OR trigger** (GloFAS ≥3,132 m³/s OR Google GRRR ≥1,195 m³/s at Wuroboki) for the riverine action, plus a flash-flood observational trigger (FloodScan SFED per-LGA for 6 BAY LGAs). The 2026 version:

1. **Action trigger** — replaced single-station OR logic with a multi-gauge consensus trigger (≥6 of 10 GRRR gauges at 4-yr RP).
2. **Readiness trigger** — new GloFAS component: reforecast ensemble mean OR latest reanalysis, both vs 3,132 m³/s. Not present in the 2025 design.
3. **NHF flash flood** — retargeted from 6 LGAs to 4 (Mobbar, Maiduguri, Jere, Geidam; only Maiduguri carries over) with new framework-document exposure thresholds; monitoring live daily at 01:30 UTC with a new 80% advisory tier.
4. **Geographic scope** — Benue state, briefly scoped as a second target state in an earlier sync, was dropped; the 2026 riverine framework covers the 7 Adamawa LGAs only.
5. **Email dispatch** — moved from raw SMTP to Listmonk/ocha-relay (new since the prior sync of this page).
6. **GloFAS reanalysis OR-branch** — the prior sync's `[conflict]` (in the doc, absent from the code) is closed: commit 5d44ff8 implements it. The prior sync's added claim that the repo analysis had *rejected* it is unsourced and has been dropped.
7. **Monitoring season** — narrowed from Jul–Nov (2025, stated in the PDF) to Aug–Nov, matching the wet-season window the 2026 calibration uses.

*(Since the prior sync of this page — branch `feat/glofas-country-reforecast-download`, SHA `fcbe009` — this sync is drawn from `feat/2026-trigger-monitoring`, SHA `0b763c3`: substantial monitoring/dispatch progress, but the design notebooks and NHF retargeting artifacts that branch had are not present here; see discrepancies.)*

## Open questions / known issues

1. **(resolved 2026-08-10)** The 2026 monitoring is live from `main` (see Monitoring). Remaining follow-ups: migrate the full distribution list into the prod Listmonk lists when the team is ready (`setup_nga_listmonk_lists.py`), link the framework document in `action.html` once published, and decide whether the data plane should move off dev-stage.
2. **Design notebooks unreachable from this branch.** The notebooks/session notes that derived the action and readiness thresholds are not in this checkout; they live on `feat/niger-benue-multistate-monitoring` (QA confirmed `exploration/2026/cerf/workflow/01`–`07`, `exploration_nhf.ipynb` and `notes/2026-06-18.md` are all there, and that their figures match `src/constants.py`). A merge or a sync against that branch would put design and implementation back in one place.
3. **NHF LGA set unresolved, now with less visibility than before.** No NHF-specific notebook exists on this branch at all, and the 2026 design work is entirely CERF-riverine — nothing in `evaluate_trigger` evaluates the NHF flash-flood window. Locate the retargeting work (`exploration_nhf.ipynb` on the branch above) before treating any NHF LGA list as current.
4. **hybas_1120840690 outlier gauge.** 4-yr threshold 142.7 m³/s (≈8× lower than the other nine) — retained by design (one vote of ten can't dominate ≥6/10) but flagged for review in any future gauge-set revision. The design note adds that it also peaks *after* the FloodScan peak (+2 d lag) and contributed to only 4 of the 6 reanalysis fire years.
5. **Data plane pinned to dev regardless of `STAGE`.** `check_forecasts.py`, `etl.get_database_forecast` and the GloFAS blob container all hardcode `dev`; going live in production requires addressing this separately from the email-routing `STAGE` flag.
6. **Multiple unmerged branches touching this framework.** Four (`feat/2026-trigger-monitoring` #37, `feat/niger-benue-multistate-monitoring`, `feat/glofas-country-reforecast-download` #35 → also merged into the second, `chore/monitoring-2026-cron-shim` #38) — reconciling them before/at merge time is worth tracking so no design work is lost.
7. **Framework document still unpublished.** `framework_doc` stays null; no 2026 revision was findable on unocha.org or ReliefWeb as of 2026-08-08 (the published doc is still the 2025-08-11 one, itself stated as valid for 2025 **and 2026**). Once a 2026 PDF exists, re-sync to confirm the funding envelope, target population, implementing agencies, `valid_until`, and whether the doc really mandates the readiness reanalysis branch.
8. **Listmonk template migration half-done.** The three campaign templates still extend a local `base.html` with its own header and contact footer, despite the dispatch code documenting them as content fragments over the Listmonk instance template — recipients may get a duplicated header/footer.
