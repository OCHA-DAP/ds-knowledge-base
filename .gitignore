.DS_Store
__pycache__/
*.pyc
.venv/
.ipynb_checkpoints/

# Internal Drive store — the MANIFEST (metadata catalog, ~9k entries) AND the
# CONTENT (full-text extracts + summaries), both classified `internal` per
# docs/PRIVACY.md (D44b/D45). MUST NOT be committed to this PUBLIC repo; durable
# internal copy is on blob / a private repo. The public repo carries only a
# POINTER at infrastructure/drive-index.md.
drive/
